language: node_js
node_js:
  - "stable"
cache:
  directories:
    - node_modules
script: npm run $JOB
jobs:
  include:
    - stage: check
      env: JOB=test
    - stage: check
      env: JOB=coverage
